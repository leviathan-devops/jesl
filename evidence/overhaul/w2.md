# WAVE 2 AUDIT — ALGORITHM CARDS + LEXICON + DEFAULT PROFILE + THE MOCK-SPLIT FIX
**Wave:** wave-1788449322223-34cc4b · **Audited:** 2026-09-03 19:55 · **Auditor:** orchestrator (mechanical re-verify, all outputs re-run personally)

## VERDICTS

### Hunk 1 — w2-algorithm-cards: 10 JSON cards in jesl/algorithms/ — CORRECT (after 3 honest fixes)
- WHAT: guard, retry, fan, oracle, bracket, ask, chain, pre-arm, verify, ship (.json), each $schema trident-workflow-v1, tier 1, meta.slots [WORK SLOT] annotation.
- WHY: Wave 2 spec S-10; first consumers proving the 37-kind catalog casts end-to-end.
- HOW VERIFIED: re-cast all 10 personally via `bun run cli/main.ts run algorithms/<name>.json` → **10/10 PASS exit 0**; `grep -c '.stub'` across all casts → **0**.
- AGENT OVER-CLAIMS CAUGHT (prose outran evidence, work was still real):
  1. Agent claimed cards cast PASS as shipped — TRUE only because cli/handlers.ts resolved missing kinds to PASS-stubs (the mock-split, hunk 3). After the fix, 3 cards failed honestly and were restructured by the orchestrator.
  2. Agent's "replay-source maps to stub PASS" note — TRUE and was the smoking gun.
- ORCHESTRATOR FIXES to the agent's cards: oracle.json (oracle-discharge requires cap `oracle:read` which the CLI never binds → restructured to math-eval→gate assert $.result eq 42, WORK SLOT documents the oracle-discharge swap under a cap-binding driver; CHANNEL-UNSET taught the rule `inbound[via]=outputs[via]`), bracket.json (claim-gate requires a fresh source_change for the subject → added the node's DESIGNED preSource mechanism, claim-gate.ts:53), chain.json (replay-source INCONCLUSIVE/EMPTY without a real runId → shipped pipeline work-slot, WORK SLOT documents the replay-source swap with config.runId).

### Hunk 2 — w2-lexicon-profile: lexicon.ts + lexicon.json + profiles/default.ts — CORRECT
- WHAT: lexicon.ts (26L, node:fs legal at jesl/ root) scans ALL_KINDS + fixtures/*.json + algorithms/*.json → lexicon.json {kinds:[{kind,family,cards}],cards,generatedAt}; profiles/default.ts (14L) DomainModule {name:"default",caps:[Shell,Fs,Http],kinds:ALL 37,defaultTier:1,brackets:{}} with module-level validateDomainModule assertion.
- HOW VERIFIED personally: `bun run lexicon.ts` → "37 kinds, 19 cards"; re-run idempotent; algorithms-dir-missing degrade verified by agent (37 kinds, 9 cards, exit 0); validateDomainModule → []; tsc 0; battery 424/424 (re-run by orchestrator post-fix).
- VERDICT: CORRECT. Zero modifications outside the 3 files.

### Hunk 3 — THE MOCK-SPLIT DEFECT (found by orchestrator audit, NOT by the agents) — FIXED, CORRECT
- WHAT: cli/handlers.ts buildNodeHandles resolved kind→impl from a hardcoded 15-kind map; every other kind — INCLUDING ALL 20 WAVE-1 IMPLEMENTATIONS — fell to an `else` branch returning a PASS-stub (`${n.type}.stub`, state PASS). The CLI production path never touched the registry. Additionally nodes/index.ts never imported the 15 self-registering Wave-1 files, and 4 infra files (replay-source, sqlite-sink, machine, workflow-machine) never self-registered at all (the infra agent's wiring claim was FALSE — over-claim caught).
- WHY IT MATTERS: the mock-split class — tests exercised real impls, the live path fed stubs. Every card PASS through a Wave-1 kind was theatrical. The old S-1 grep (INCONCLUSIVE in nodes/) could never catch it because the stub said PASS.
- THE FIX (3 edits, all by the orchestrator):
  1. core/registry.ts: +getRegisteredImplSync(kind) accessor over globalRegistryStore (the designed single source of truth).
  2. nodes/index.ts: imports/exports ALL 37 real impls; replaceStubSync loop wires all 21 non-self-registering kinds (12 originals + 5 pattern + 4 infra); stubNodes filter computed from the real set.
  3. cli/handlers.ts: buildNodeHandles resolves via getRegisteredImplSync; hardcoded map + prompt special-case + PASS-stub else branch DELETED (no backward-compat preservation); unknown kind → loud [JESL UNKNOWN-NODE] throw (frozen token, D15).
- REGRESSION DURING FIX (caught + fixed): first attempt broke 3 cli.test.ts tests (gate resolved to registry STUB — the original 12 full nodes were ALSO never registered). Completed the wiring in index.ts → 424/424.
- ORDER SAFETY: checkUnbracketed (:188) runs BEFORE buildNodeHandles (:243) → bad-unbracketed.json still emits its frozen token (verified: cli tests green).
- HOW VERIFIED: tsc 0; 424/424 (39 files, re-run twice); 10/10 card casts PASS; `.stub` count across all casts = 0; evidence patterns now real (gate.assert, claim-gate EVIDENCED via preSource, ratio-classifier.suppress, escalation-memory.track, audit-registry, replay-source, file-io.write, math-eval EVALLED); manifest f77b448f unchanged.

## COVERAGE MAP (Wave 2 criteria)
| Criterion | Status | Evidence |
|---|---|---|
| S-10 cards cast cleanly | ✅ 10/10 PASS exit 0 | orchestrator re-cast, this session |
| S-11 lexicon ≥37 entries | ✅ 37 kinds, 19 cards | bun run lexicon.ts, idempotent |
| S-12 default profile validates | ✅ validateDomainModule → [] | agent run + tsc |
| S-2 tsc 0 | ✅ | re-run post-fix |
| S-3 battery green | ✅ 424/424 (39 files) | re-run post-fix |
| S-7/S-8 manifest | ✅ f77b448fff1e...8ff1c | recomputed post-wave |
| No Llm wiring | ✅ 0 prompt nodes in cards | grep '"type": "prompt"' → 0 |

## HONEST RESIDUALS
- cli.test.ts tests still pass via the registry path — the test driver and CLI now share ONE resolution path (the registry), which is the point.
- prompt/shadow-agent/subagent-dispatch are not in ALL_KINDS; buildNodeHandles now throws UNKNOWN-NODE for them if a doc passes validateDoc (whose known-set still lists them — pre-existing drift, out of Wave 2 scope, flagged for Wave 4).
- The 3 restructured cards document cap-bound variants in meta.slots (oracle-discharge, prompt tier-2, replay-source) — the templates cast PASS under CLI caps as shipped.
