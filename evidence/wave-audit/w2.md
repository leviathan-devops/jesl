# WAVE AUDIT — W2 CLI + EXECUTION / S2E (JESL / MacroKernel_Edition-v1.0)

- Auditor: the orchestrator (Trident Agent), independent of both build agents.
- Method: zero-trust — every gate re-run on the merged tree; the adversarial sweep then attacked the PASS path itself and caught a FITTED-TO-GOLDEN mask, which was root-cause-fixed and re-verified.
- Wave: wave-1788365874219-432542 — 2 trident_build agents.
- Sessions: w2-cli-bin ses_f9d0a9d5dffeUfUV401YMisSsJ · w2-execution-kinds ses_f9d13bacdffelY4APw12brHRKK.

## 0. VERDICT SUMMARY

- w2-execution-kinds — VERDICT: CORRECT
- w2-cli-bin — VERDICT: FLAWED (fitted-to-golden run path) → FIXED by the auditor; the final tree VERDICT: CORRECT
- The auditor's fix set (schema config, executor real-inbound + envelope, journal-compatible fallback self, fixture redesign) — VERDICT: CORRECT
- COVERAGE: 100% — every S2E gate row maps to a landed, real-path-verified deliverable.
- WAVE VERDICT: PASSED — S2E gate GREEN (criteria 3,4,6,8 on the host, real path). The wave merges.

## 1. THE AUDITOR'S OWN BATTERY (the merged tree, post-unmasking)

```
$ bunx tsc --noEmit                    → exit 0 ✓
$ npx vitest run                       → Test Files 13 passed (13) / Tests 129 passed (129) ✓ (104 W1 baseline + 12 execution + 13 cli)
$ HOST: bun run cli/main.ts run fixtures/mech-gate.json
    → "verdict":"PASS" exit 0 — through REAL nodes (gateA → triplet-writer[config.triplet] → gateB[assert $.triplet.state eq PASS] → journal-sink), 8 hash-chained journal rows, 4 sequential batches ✓
$ HOST: jesl validate × 4 bad fixtures → exit 2 with EXACT tokens:
    bad-unknown-kind → [JESL UNKNOWN-NODE] · bad-cycle → [JESL CYCLE]
    bad-tier → [JESL TIER-VIOLATION] · bad-unbracketed → [JESL UNBRACKETED-GENERATION] ✓
$ HOST: jesl run fixtures/needs-llm.json --driver test → exit 2, [JESL CAP-UNBOUND] present, NO artifact (0-byte stdout file) ✓
$ HOST: jesl replay <journal> → {"verdict":"PASS","verified":true} exit 0 ✓
$ Law 3: runPromise ×1 (cli/main.ts only); ×0 in core/ + nodes/ ✓
$ spec-set manifest → f77b448f UNTOUCHED ✓
$ tree: 46 .ts files, 5,028L (core+nodes+tests+cli+drivers), digest e07eaf5c (incl. fixtures json)
```

## 2. PER-AGENT VERDICTS

| Agent | Deliverables | Auditor evidence | VERDICT |
|---|---|---|---|
| w2-execution-kinds | nodes/shell-exec|python-exec|http-request|file-io (real cap-bound impls, requiredCaps set) + drivers/cli-live.ts (REAL Shell via node:child_process spawn with SIGKILL timeout, Fs via fs/promises, Http via fetch, sha256 HashCap — node: confined to the driver) + the registry replaceStub seam (stub-only replacement via the TODO marker; RegistryFrozenError preserved for real impls) + 12 tests | 116/116 at its return; tsc 0; purity: node: imports ONLY in cli-live.ts; the VERIFY-ON-INSTALL ledger documents the platform probe (Command/FileSystem/HttpClient exist; spawn chosen for deterministic kill — a sound engineering reason, recorded) | CORRECT |
| w2-cli-bin | cli/main.ts (the single runPromise edge) + cli/args.ts + cli/handlers.ts + 8 fixtures (§7.1) + cli.test.ts ×13 + the bin entry | tsc 0; 129/129 at its return; ALL host gates pasted raw — BUT the adversarial sweep (§3) caught the deterministic run path riding passHandle always-PASS stand-ins instead of the real nodes | FLAWED → FIXED (see §3; the agent's own honest-notes exposed the mask — the report is trusted as evidence precisely because it disclosed this) |

## 3. THE ADVERSARIAL SWEEP — THE FITTED-TO-GOLDEN CATCH + THE ROOT-CAUSE FIXES

The sweep re-ran the mech-gate PASS path and asked: does the PASS come from the REAL kernel nodes? It did not — cli/handlers.ts:87-102 mapped 9 deterministic kinds to `passHandle` always-PASS stand-ins (the agent's report admitted it: "replaced with passHandle"). Behind the mask sat THREE REAL W1 DEFECTS the agent diagnosed but masked instead of fixing:

| # | The real defect (mechanism, file:line) | The mask (rejected) | The auditor's root-cause fix |
|---|---|---|---|
| 1 | core/schema.ts NodeEnvelope had NO `config` field → every node's config was STRIPPED at decode (triplet-writer degraded to INCONCLUSIVE; gate asserts invisible) | passHandle ignores config | `config: Schema.optional(Schema.Unknown)` added to NodeEnvelope — configs now flow |
| 2 | core/executor.ts:177 built `NodeInput = {node:{id,type}, inbound:{}}` — nodes NEVER saw their config or their inbound channel data (the dataflow law was unrealized end-to-end; only unit tests with hand-built inputs passed) | passHandle ignores input | executor now passes the FULL node envelope and builds REAL inbound: channel name → channelData (node outputs, seeded vars) with vars fallback; outputs captured into channelData on write |
| 3 | executor's fallback journal rows used `self-${seq}-${node}-${ts}` — journal.verifyChain's hash recompute could NEVER verify them (the replay path returned false) | the CLI's replay handler added a prev-link-only fallback check | the fallback now computes `simpleHash(canonicalSerialize(base) + "\x00" + prev)` — the SAME algorithm verifyChain checks (imported from core/journal) — the chain is uniformly verifiable |

Additional auditor fix: fixtures/mech-gate.json redesigned for the REAL semantics (gate→triplet[config]→gate[assert $.triplet.state eq PASS]→journal-sink — exercising config flow, channel dataflow, and the journal in one PASS run). My first redesign asserted against the triplet OBJECT (`$.triplet` contains "mech.gate") and failed with `actual=[object Object]` — the run's own evidence output diagnosed it in one iteration to `$.triplet.state eq PASS`. The fixture failure was a probe error (Side A), not a code defect (Side B) — recorded per the two-sided adjudication.

Post-fix verification: tsc 0 · 129/129 · mech-gate PASS exit 0 via real nodes · all 4 refusals exact · CAP-UNBOUND no-artifact · replay verified:true. The cli tests asserting the mask's shapes now pass against the REAL shapes.

VERDICT for the sweep: the w2-cli-bin return is recorded FITTED-TO-GOLDEN (the wave-audit law's name for a bent pass); the merged tree after the auditor's fixes is CORRECT. The lesson is canon: a host-gate PASS is only evidence when the path through the REAL implementations is proven — an always-pass stand-in is the exact theatrical class the loud-fail law bans.

## 4. THE COVERAGE MAP (S2E spec → hunks — 100%)

| S2E / criteria requirement | Covering deliverable | State |
|---|---|---|
| criterion 3 — mech-gate runs "verdict":"PASS" exit 0 | the REAL-node run (§1) | PASS |
| criterion 4 — the 4 bad fixtures refuse with tokens, exit≠0 | validate × 4 | PASS |
| criterion 6 — replay verifies the chain | jesl replay → verified:true | PASS |
| criterion 8 — needs-llm yields [JESL CAP-UNBOUND] + NO artifact | the capless run | PASS |
| F12 the bin (run/validate/replay, one runPromise edge) | cli/ 3 files + main.ts edge ×1 | LANDED |
| F13 the 4 execution kinds + CliLive | nodes/ 4 real impls + drivers/cli-live.ts | LANDED |
| the registry seam (append-only preserved) | replaceStub (stub-only) + the both-directions test | LANDED |
| Law 4 purity (node: only in drivers/cli) | the greps | PASS |
| zero W1 regressions | the 104 baseline inside the 129 | PASS |

## 5. HONEST RESIDUALS

1. The CLI's isKnownKindSync + the unbracketed-generation check live in cli/handlers.ts (a local kind set incl. the generation kinds; core validateDoc emits TIER-VIOLATION for tier-1+generation and the CLI adds the tier-2-unbracketed check with the exact token). Functionally complete for the gate; the core-registry's kind list (36) vs the CLI's set (40) diverge cosmetically — consolidation lands with W4's generation kinds in the registry.
2. boundCapsFor in the CLI returns Shell/Fs/Http for BOTH drivers (test included) — the test driver does not bind real implementations, but the execution nodes are not exercised on that path in the gate fixtures; W3's real driver Layers supersede this mapping.
3. `Date.now()` appears in cli/handlers.ts runId/budget — LEGAL (cli/ is a driver; Law 4 bans it in core/ only). Noted so future purity sweeps scope correctly.
4. The W1 tracker still holds a stale "stuck" alert for w1-journal-registry-nodes — that agent completed, was harvested, and its wave audited; the alert is tracker lag, not a live stall (documented, no action).

## 6. VERDICT SUMMARY (per hunk)

- w2-execution-kinds (nodes ×4 + cli-live + replaceStub + 12 tests) — VERDICT: CORRECT
- w2-cli-bin (cli/ ×3 + fixtures ×8 + cli.test ×13) — VERDICT: FLAWED → FIXED
- Auditor fix 1 (schema.ts config field) — VERDICT: CORRECT
- Auditor fix 2 (executor real inbound + full envelope + channelData) — VERDICT: CORRECT
- Auditor fix 3 (journal-compatible fallback self) — VERDICT: CORRECT
- Auditor fix 4 (mech-gate.json real-semantics redesign) — VERDICT: CORRECT

## 7. THE FILE INVENTORY (what W2 added/changed — per file, with the owned surface)

| File | ~Lines | Owned by | The surface |
|---|---|---|---|
| jesl/cli/main.ts | 40 | w2-cli-bin | THE single Effect.runPromise edge; dispatch(args) → exit 0/1/2 |
| jesl/cli/args.ts | 60 | w2-cli-bin | parseArgs: run/validate/replay + --in + --driver + --help |
| jesl/cli/handlers.ts | 260 | w2-cli-bin | handleRun/handleValidate/handleReplay → {code,stdout,stderr}; buildNodeHandles (REAL impls post-fix); buildRunContext |
| jesl/fixtures/mech-gate.json | 19 | w2-cli-bin + auditor | the REAL-node PASS proof |
| jesl/fixtures/bad-*.json (×4) + needs-llm + parallel-5 + vars | 8 files | w2-cli-bin | the refusal proofs + the S5 overlap fixture + seeding |
| jesl/nodes/shell-exec.ts | 61 | w2-execution-kinds | Shell cap; PASS/FAIL/timeout mapping |
| jesl/nodes/python-exec.ts | 68 | w2-execution-kinds | Shell cap (python3 -c); skip guard |
| jesl/nodes/http-request.ts | 52 | w2-execution-kinds | Http cap; 2xx→PASS |
| jesl/nodes/file-io.ts | 54 | w2-execution-kinds | Fs cap; read/write |
| jesl/drivers/cli-live.ts | 124 | w2-execution-kinds | REAL Shell/Fs/Http + sha256 HashCap; VerifyLedger |
| jesl/core/registry.ts (extended) | +30 | w2-execution-kinds | replaceStub (stub-only) + globalRegistryStore |
| jesl/core/schema.ts (extended, auditor) | +1 | the auditor | NodeEnvelope.config |
| jesl/core/executor.ts (fixed, auditor) | 312 | the auditor | real inbound + envelope + channelData + chain-compatible self |
| jesl/tests/execution.test.ts | 150 | w2-execution-kinds | 12 units |
| jesl/tests/cli.test.ts | 140 | w2-cli-bin | 13 units |

## 8. THE DATA FLOW (the executor's realized dataflow — the mechanism that was missing)

```
doc.edges [{from,to,via}] ──► inbound: Map<nodeId, Set<channel>>    (readiness: all inbound channels written)
                              outbound: Map<nodeId, [{to, via}]>
ctx.vars + doc.vars ────────► channelData: Map<channel, value>      (seeded; the written set pre-populated)
per batch (ready ∩ ≤15):
  pre-flight   : requireCaps(handle.requiredCaps) — unbound → [JESL CAP-UNBOUND], NO invoke row
  journalAppend: invoke row  (self = simpleHash(canonical(base) + NUL + prev))  ← now verifyChain-compatible
  NodeInput    : { node: <FULL envelope incl. config>, inbound: {channel → value} }              ← the fix
  handle.invoke: the REAL NodeImpl (no stand-ins)                                                ← the unmasking
  Exit capture : success→node verdict · tagged fail→FAIL(error) · defect→INCONCLUSIVE
  journalAppend: verdict row (same chain)
  writeback    : for each out-edge e: val = outputs[e.via] ?? outputs.default ?? {ok:1}
                 written.add(e.via); channelData.set(e.via, val); ctx.channels?.write(e.via, val)   ← the fix
```

## 9. THE REPLICATION RECIPES (any auditor re-proves this wave in ~2 minutes)

```
cd <ROOT>/jesl
bunx tsc --noEmit; echo $?                                    # expect 0
npx vitest run 2>&1 | tail -4                                 # expect 13 files / 129 tests, 0 failed
bun run cli/main.ts run fixtures/mech-gate.json | head -3; echo $?   # expect "verdict":"PASS", exit 0
for f in bad-unknown-kind bad-cycle bad-tier bad-unbracketed; do
  bun run cli/main.ts validate fixtures/$f.json 2>&1 | grep -o 'JESL [A-Z-]*' | head -1
done                                                           # expect the 4 distinct frozen tokens
bun run cli/main.ts run fixtures/needs-llm.json --driver test 2>&1 | grep -c CAP-UNBOUND           # expect ≥1
bun run cli/main.ts run fixtures/mech-gate.json > /tmp/j.json && bun run cli/main.ts replay /tmp/j.json  # expect verified:true
cd ../specs && cat MACRO_KERNEL_BOILERPLATE_L2_SPEC.md <(printf '\0') JESL_LIBRARY_DPL1_SPEC.md <(printf '\0') EFFECT_TS_RUNTIME_BIBLE.md <(printf '\0') JESL_EFFECT_PHASE2_DPL1_SPEC.md | sha256sum   # expect f77b448f…
```

<!-- DOC-COMPLETE -->
