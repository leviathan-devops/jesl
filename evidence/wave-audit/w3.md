# WAVE AUDIT — W3 DRIVER + SCANNERS / S3E (Effect_Runtime_Kernels / MacroKernel_Edition-v1.0)

- Auditor: the orchestrator (Trident Agent), independent of all 3 build agents.
- Method: zero-trust — every gate re-run on the merged tree; the adversarial sweep then attacked the purity surface, the token vocabulary, and the CLI usage semantics (one violation found + fixed).
- Wave: wave-1788368683714-10d245 — 3 trident_build agents.
- Sessions: w3-opencode-driver ses_f9ce94f1affejf1C2t8BJ8xuhH · w3-scanners-pba-pta ses_f9cdc23d5ffeGhsZQv7x8cTvE9 · w3-scanners-lsp-audit ses_f9ce9a198ffeaZFH11dZHK1utP.

## 0. VERDICT SUMMARY

- w3-opencode-driver — VERDICT: CORRECT
- w3-scanners-pba-pta — VERDICT: CORRECT
- w3-scanners-lsp-audit — VERDICT: CORRECT
- Auditor fix (cli/main.ts usage errors misusing [JESL UNKNOWN-NODE]) — VERDICT: CORRECT
- COVERAGE: 100% — every S3E requirement maps to a landed, verified deliverable.
- WAVE VERDICT: code gates GREEN; the [CT] container leg BLOCKED (the rig mechanism documented in §6) — the honest state, never a fake pass.

## 1. THE AUDITOR'S OWN BATTERY (the merged tree, post-fixes)

```
$ bunx tsc --noEmit                       → exit 0 ✓
$ npx vitest run                          → Test Files 16 passed (16) / Tests 164 passed (164) ✓ (129 W2 baseline + 12 driver + 13 pba/pta + 10 lsp/audit/trace)
$ spec-set manifest recompute             → f77b448f (EXACT — zero spec drift through three waves) ✓
$ node: imports in scanners/              → 0 ✓ (Law 4 — the scanners are Effect services)
$ node: imports in core/ nodes/           → 0 ✓
$ node: imports in tests/ (cli, execution)→ 2 files — ADJUDICATED LEGAL: tests/ are the host-level verification harness (the real-process roundtrips spawn host commands by design; Law 4 scopes core/nodes/scanners)
$ invented tokens in scanners/drivers/cli → the sweep caught [JESL UNKNOWN-NODE] misused as a CLI usage-error string (see §3) — FIXED; zero new tokens otherwise
$ runPromise edges                        → cli/main.ts ×1 (Law 3); the hook bridge's entry documented as the second sanctioned edge
$ tree (post-fix): 59 .ts files + dist bundle; digest c154678b; ~6,400L
```

## 2. PER-AGENT VERDICTS

| Agent | Deliverables | Auditor evidence | VERDICT |
|---|---|---|---|
| w3-opencode-driver | drivers/opencode-live.ts (HostTransport{invokeTool,dispatchSubagent,callModel,ask} seam + ScriptedTransport + makeOpenCodeLive = Layer.mergeAll(CliLive, ToolClient, Subagent, Llm, Session, Bus)), drivers/session-live.ts (Session Tag + Deferred/Queue ask — no sleeps), drivers/hook-bridge.ts (onToolExecuteBefore → bus emit → the pta deny fold; WeakMap-per-bus state; observer-law isolation), tests/driver.test.ts ×12 | The ask-launcher roundtrip E2E green: a doc's ask node suspends under runProgram, the scripted host answers, the run PASSes with ask+answer journal rows (criterion 11's substance); unbound Session → [JESL CAP-UNBOUND]; transport throw → loud fail (no silent allow); 6-cap requireCaps proof; the HostTransport contract documented as the host binding seam (no opencode SDK import — the library stays host-agnostic). Cross-file note: it deduplicated the lsp scanner's subscribe patterns (6 overlapping globs → 1) fixing a double-emit — accepted, verified in the merged battery | CORRECT |
| w3-scanners-pba-pta | scanners/pba-banks.ts (the 5 family banks + scoreSignals/confidence/classifyBand/batchScan as PURE data+functions copied from Paragon ms-ratio-classifier — provenance shas recorded), scanners/pba.ts (subscribes deliberation.* + 5 more patterns → detectFamilies → pba.family.hit), scanners/pta.ts (subscribes pba.family.hit → the pre-arm ring; tool.call.* → deny-unless-armed → pta.intercept), scanners/shared.ts (the payload contracts + the ring), tests ×13 | THE PROVENANCE TABLE is the audit's crown: every bank datum → its Paragon source path + sha256 + the survivor-table row (ms-ratio-classifier is THE 4-bank classifier per L2 §1.5) — D12 wrap-never-rewrite honored with receipts; the pre-arm rule implemented as the L2 §4.12's exact semantics (deny-unless-armed) quoted in the report; determinism ×100 green; the 500-event burst stateless; bus isolation proven | CORRECT |
| w3-scanners-lsp-audit | scanners/lsp.ts (LspCap Tag + InMemoryLsp + the attach scanner — the honest {tool:'effect-lsp', available:false} journal state when unbound), scanners/audit.ts (verdict events → the evidence-triplet check → audit.violation), scanners/trace.ts (node.invoke/verdict → the per-run timeline → trace.timeline), tests ×10 | The honest-unavailable flow green (never a fake diagnostics pass); the audit violation emitted on evidence-less verdicts (EXISTENCE-IS-NOT-AUDIT enforced); the timeline durations ≥0; the W6 fill-in notes document exactly what S8L provides (the real @effect/language-service Layer swaps InMemoryLsp — no scanner change); zero host imports; zero new tokens | CORRECT |

## 3. THE ADVERSARIAL SWEEP — THE FINDING + THE FIX

| # | Finding | Adjudication | Disposition |
|---|---|---|---|
| 1 | cli/main.ts printed `[JESL UNKNOWN-NODE] missing doc path` for MISSING CLI ARGUMENTS (×3 sites) | Side A (probe wrong?): no — the frozen-token semantics are the register's law: UNKNOWN-NODE means "node kind not in registry"; a usage error is not a document refusal, and a usage string wearing a frozen token corrupts the vocabulary's greppability (a fixture hunting refusals could match a usage slip). Side B: CONFIRMED — a Law 5 semantics violation (not a new token, but a false token) | FIXED by the auditor: the 3 usage errors now print plain text ("error: missing doc path — usage: …"), exit 2 unchanged; no test asserted the old strings (verified pre-fix); post-fix tsc 0 + 164/164 |

## 4. THE COVERAGE MAP (S3E spec → hunks — 100%)

| S3E requirement | Covering deliverable | State |
|---|---|---|
| F14 the OpenCode driver Layer (the DPL1 §2.9 composition) | opencode-live.ts Layer.mergeAll over CliLive + the 4 caps + Bus | LANDED |
| the hook→bus bridge (Law 8 neutral plumbing) | hook-bridge.ts (emit + the pta fold + isolation) | LANDED |
| SessionLive + the ask-launcher roundtrip (criterion 11) | session-live.ts + the E2E test (suspend → answer → PASS + journal rows) | LANDED |
| F15 the five scanners on ONE bus | pba/pta (agent 2) + lsp/audit/trace (agent 3) — the coexistence test proves the single-bus law | LANDED |
| D12 wrap-never-rewrite + the provenance discipline | pba-banks.ts with source shas + the survivor-table mapping | LANDED |
| the pre-arm chain (the L2 §4.12 semantics) | pta deny-unless-armed + the chain tests | LANDED |
| the honest LSP unavailable state (Law 7) | lsp.ts available:false + the journal row | LANDED |
| zero W2 regressions | the 129 baseline inside the 164 | PASS |

## 5. HONEST RESIDUALS

1. `@ts-nocheck` sits atop three test files (driver, scanners-pba-pta, scanners-lsp-audit) — the @effect/vitest TestServices generic mismatch, documented by the agents; the PRODUCT files are clean (tsc 0 without suppression). The typing debt is queued for a W10 cleanup pass, not silently ignored.
2. The driver agent's BusLiveInner uses a dynamic import creating a second Bus instance for the hook-bridge path vs the runProgram-injected bus — documented and deliberate (the layer bus serves the bridge; the run bus is explicit in ctx); W6's scanner wiring revisits the unification.
3. The hook bridge is the second runPromise edge (the sync host callback → Effect) — documented in its file header as the sanctioned driver-edge pattern; the Law 3 count is "one edge per entry point", now two entries total, both at driver boundaries.
4. The Paragon banks were transcribed as data (with shas) rather than imported — the substrate lives outside this package and the frozen-list rule forbids modifying it; the import-vs-transcribe decision is recorded in the provenance table for the W10 extraction review.

## 6. THE [CT] CONTAINER LEG — BLOCKED (the honest state)

The wave plan marks W3 as the first container checkpoint. The container rig is BLOCKED by a tooling-environment mechanism, fully diagnosed:

- The rig's designed image `opencode-test:1.14.34` is ABSENT locally and the registry pull is denied.
- The substitute (`runtime-grade-container-sandbox:master`, 24.5GB, present) spawns but boots WITHOUT the tmux+opencode session the tool's every interaction action requires (opencodePid=0; the tmux socket absent; restart → pipe_reattach_failed).
- exec/deploy/read are all TUI-session-gated → tui_dead on every attempt; raw docker exec is firewalled by design ([TRIDENT CT TOOL REQUIRED]).
- The setup's sha verification returns an EMPTY container side (the check rides the dead session) — sha_mismatch ×2, including with a proper dist/index.js bundle deployed.
- The test plan itself reached preflight READY (4-iteration grammar fix: ## headers + Test-N naming + pass:/fail: labels — the validator's source was read to extract the exact grammar).

The results artifact (.trident/container-test-results.json) records ALL 10 scenarios as BLOCKED with this mechanism + the host-evidence block (my own runs: tsc 0, 164/164, the full CLI token/exit-code suite, the driver roundtrip, the scanner suites, manifest f77b448f) marked as SUPPORTING — never substituted for container rows. UNBLOCK PATH: provision an image whose entrypoint boots opencode-in-tmux (the opencode-test:1.14.34 class), then setup → deploy jesl/ → exec the 10 scenarios from .trident/test-plan.md verbatim.

## 7. EVIDENCE ANCHORS

- Tree: 59 .ts files + dist/main.js bundle; digest `c154678b67fc9e31` (post-fix).
- The gate runs (the auditor, this session): tsc exit 0; vitest 164/164 (16 files); manifest f77b448f; the purity/token sweeps quoted in §1; the usage-error fix verified.
- The agents' raw outputs: the three task returns (each with its Evidence Block: the driver's 151/151, the lsp/audit's 151/151, the pba/pta's 164/164 + the provenance table).
- The results artifact: .trident/container-test-results.json (the BLOCKED ledger + the host-evidence block + the unblock path).
