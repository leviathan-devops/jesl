# WAVE AUDIT — W4 GENERATION / S4E (JESL / MacroKernel_Edition-v1.0)

- Auditor: the orchestrator (Trident Agent), independent of both build agents.
- Method: zero-trust — every gate re-run on the merged tree; the adversarial token sweep caught an invented 9th token, fixed and re-verified.
- Wave: wave-1788373930190-bb146c — 2 trident_build agents.
- Sessions: w4-prompt-activity ses_f9c979adfffeOqtO1B60lR8LKN · w4-jesl-workflow ses_f9c8f97d3ffepjZgEKu2QLIOCJ.

## 0. VERDICT SUMMARY

- w4-prompt-activity — VERDICT: CORRECT
- w4-jesl-workflow — VERDICT: CORRECT (one token-shape slip fixed by the auditor, see §3)
- COVERAGE: 100% — every S4E requirement maps to a landed, verified deliverable.
- WAVE VERDICT: PASSED — S4E gate GREEN (tsc 0 · 184/184 · the no-re-pay proven · tokens clean after the fix).

## 1. THE AUDITOR'S OWN BATTERY (the merged tree, post-fix)

```
$ bunx tsc --noEmit                       → exit 0 ✓
$ npx vitest run                          → Test Files 18 passed (18) / Tests 184 passed (184) ✓ (164 baseline + 10 prompt + 10 workflow)
$ spec-set manifest recompute             → f77b448f (EXACT — zero spec drift through four waves) ✓
$ token sweep on workflow/ + prompt.ts    → [JESL CAP-UNBOUND] ×2 (the frozen refusal, legit) · [JESL UNBRACKETED-GENERATION] ×1 (validate-time, legit) · [JESL JOURNAL-CORRUPT] ×5 → FIXED to plain JOURNAL_CORRUPT strings (§3); 8/8 frozen register intact ✓
$ purity: node: imports in workflow/ + nodes/prompt.ts → 0 ✓
$ tree: 64 .ts files; digest 408f7500e63d33df
```

## 2. PER-AGENT VERDICTS

| Agent | Deliverables | Auditor evidence | VERDICT |
|---|---|---|---|
| w4-prompt-activity | nodes/prompt.ts 223L (dual-mode: llm via the Llm cap as an Activity / template pure; the bracket check loop max-2 repairs; confidence 0.55 → INCONCLUSIVE/UNCLEAR; journal pre+post rows via ctx.journal or the Journal service; replaceStubSync at init) + tests/prompt.test.ts ×10 + the deps install (@effect/workflow 0.19.1, @effect/ai 0.37.0 — the DD24 ledger records @effect/ai NOT imported: the Llm cap abstraction stands alone, the bible's LanguageModel+Tool is the upstream adapter story at the driver boundary) + the CLI reconciliation (handlers.ts imports the real node) + the registry ALL_KINDS gap patch ('prompt' kind was missing — replaceStubSync would have failed RegistryFrozenError; the append is the same class the W2 execution kinds justified) | 10/10 units proving: template-no-cap, llm PASS+confidence, bracket-1-invoke, repair-exactly-2, 3-violations-FAIL, 0.4→INCONCLUSIVE/UNCLEAR, 0.7→PASS, CAP-UNBOUND, the journal rows, the replaceStub both-directions. tsc 0; 174/174 at its return; the spec disconnect (L2 names call-model/ask-launcher modes; implemented llm/template with aliases) honestly flagged | CORRECT |
| w4-jesl-workflow | workflow/jesl-run.ts 146L (Workflow.make JeslRun — payload docHash+seed, idempotencyKey `${docHash}:${hashSeed}`, RunReceipt{runId,verdict,rowsCount,journalTail}; the restore-or-start handler: rows-present pre-check → verify loud-fail on corrupt → covers-gated replay with the summary rebuilt FROM rows → runProgram for uncovered), workflow/activities.ts 102L (makeNodeActivity → Activity.make named node:<id>; DurableDeferred ask-launcher — the ask row persists, the answer row resumes, the ask counter 1 total), tests/workflow.test.ts ×10, tsconfig include += workflow | The no-re-pay HEADLINE proven in-container-grade units: second run invoke counter 0 + covers true + verdict FROM rows; different seed executes again (the key discriminates); corrupt → loud fail; the receipt correct both runs; node:<id> sources in the rows. The VERIFY-ON-INSTALL ledger maps every DPL1 sketch field to the installed 0.19.1 surface (Workflow.make options-object, Activity.make exact match, DurableDeferred.make, executionId = hash-of-key not raw — all deltas recorded) | CORRECT (one token-shape slip — §3) |

## 3. THE ADVERSARIAL SWEEP — THE FINDING + THE FIX

| # | Finding | Adjudication | Disposition |
|---|---|---|---|
| 1 | workflow/jesl-run.ts ×5: `Effect.fail("[JESL JOURNAL-CORRUPT] …")` — an invented 9th token | Side A (probe wrong?): no — the sweep greps the exact frozen-vocabulary shape; the register (COMPACTION_SURVIVAL §13) is frozen at 8 and adding requires a D-entry + fixture updates in-wave. Side B: CONFIRMED — same Law-5 class as W3's GATE-FAIL | FIXED by the auditor: the 5 occurrences → plain `JOURNAL_CORRUPT …` failure strings (the loud-fail law requires LOUD, not token-shaped). tsc 0 + 10/10 workflow tests + 184/184 re-verified post-fix |

## 4. THE COVERAGE MAP (S4E spec → hunks — 100%)

| S4E requirement | Covering deliverable | State |
|---|---|---|
| F16 prompt dual-mode (call-model as Activity) | nodes/prompt.ts — llm via the Llm cap, journal-observed | LANDED |
| the unbracketed refusal preserved | the schema's validate-time TIER-VIOLATION/UNBRACKETED (unchanged) + the node's bracket assertion | LANDED |
| the bracket: gate + repair≤2 + confidence 0.55→UNCLEAR | the check loop + the confidence mapping — the tests prove exactly 2 invokes on repair, INCONCLUSIVE/UNCLEAR below the floor | LANDED |
| F17 Workflow.make JeslRun (docHash+seed idempotency) | workflow/jesl-run.ts + the key-format test | LANDED |
| DurableDeferred ask-launcher (answer-from-journal, never re-asked) | workflow/activities.ts + the suspend/resume test (counter 1) | LANDED |
| no-re-pay on generation replay (criterion 15) | the second-run invoke-counter-0 test + covers() true + verdict FROM rows | LANDED — the wave's headline |
| DD24 install ledger (@effect/workflow + @effect/ai) | the versions pinned (0.19.1 / 0.37.0) + the surface deltas recorded | LANDED |
| zero regressions | the 164 baseline inside the 184 | PASS |

## 5. HONEST RESIDUALS

1. The workflow's replay path uses `journal.rows(runId).length > 0` as the covered-pre-check + `verify` for corruption, with `covers(docHash,seed)` asserted in tests — the three-layer gate (rows-present → verify → covers) is broader than the DPL1 sketch's single covers() call; the delta is documented in the module and is strictly louder.
2. The prompt node's spec-mode disconnect (L2's call-model/ask-launcher vs implemented llm/template with aliases) is recorded in the agent's report; a terminology alignment pass belongs to the W10 docs sweep, not a code change now.
3. `as any` casts on the Workflow.make success/error Schema params (the Schema.All variance) — recorded in the VERIFY-ON-INSTALL ledger; typed cleanly when the upstream tightens.
4. DurableDeferred is test-mode durable (the journal rows are the substrate; the WorkflowEngine persistence activates when an engine is provided) — the honest boundary documented in activities.ts; production engine wiring is W6+ territory.

## 6. VERDICT SUMMARY (per hunk)

- w4-prompt-activity (prompt.ts + 10 tests + the install + the CLI reconciliation + the registry gap patch) — VERDICT: CORRECT
- w4-jesl-workflow (jesl-run.ts + activities.ts + 10 tests + the tsconfig include) — VERDICT: CORRECT
- Auditor fix (the 9th-token strip in jesl-run.ts) — VERDICT: CORRECT
- COVERAGE: 100%.
- WAVE VERDICT: PASSED — S4E gate GREEN.

## 7. EVIDENCE ANCHORS

- Tree: 64 .ts files; digest `408f7500e63d33df`.
- The gate runs (the auditor, this session): tsc exit 0; vitest 184/184; manifest f77b448f; the token/purity sweeps quoted in §1; the post-fix workflow suite 10/10.
- The agents' raw outputs: both task returns (each with its Evidence Block; the prompt agent's 174/174, the workflow agent's 184/184 + the VERIFY-ON-INSTALL table).
