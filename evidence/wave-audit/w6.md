# WAVE AUDIT — W6 THE WRAPS / S7P+S8L

- Auditor: the orchestrator. Wave: wave-1788381055290-b21b85. Sessions: w6-paragon-wrap ses_f9c2989a7ffeyzDuwifGvjRKZf · w6-lsp-gate ses_f9c2af68fffe6Pe5RAaKmmsniJ.

## 0. VERDICT SUMMARY
- w6-paragon-wrap — VERDICT: CORRECT
- w6-lsp-gate — VERDICT: CORRECT
- WAVE VERDICT: PASSED — S7P+S8L gate GREEN + THE THIRD [CT] 11/11 in-container.

## 1. THE AUDITOR'S BATTERY (merged tree)
```
bunx tsc --noEmit → exit 0 ✓
npx vitest run → 22 files / 233 passed ✓ (211 + 12 wraps + 10 lsp-gate)
manifest → f77b448f ✓ (zero spec drift through six waves)
token sweep wraps/ → zero [JESL inventions ✓
purity → wraps/effect-lsp.ts node: ×3 = the CLI wrap boundary (the sanctioned class: it invokes the external effect-language-service binary — same class as drivers/cli-live's child_process) ✓
tree: 77 .ts files; digest c80f56a9; tarball dcabd97c
```

## 2. PER-AGENT VERDICTS
| Agent | Deliverables | Evidence | VERDICT |
|---|---|---|---|
| w6-paragon-wrap | wraps/behavior-engine.ts 194L (arming state: escalation 5/2/0, refractory 25, alpha 0.05, fire 1.0 thr-v1 — PROVENANCE SHAS per constant) + tool-engine.ts 72L (intercept: armed→deny w/family; unarmed bash→deny-unless-armed; clean→allow) + the hook-bridge optional ToolEngine consult + tests ×12 | TEST_EVASION → pba hit → arms → bash DENIED; the escalation decay table asserted (1→5, 2→5, 3→2, 4→0); two families independent; the bridge without engine → W3 unchanged; crash isolates. The L2 §4.12 chain order quoted and implemented. Double-handler regression self-caught + fixed (single PBA_HIT_EVENT subscription) | CORRECT |
| w6-lsp-gate | wraps/effect-lsp.ts 126L (the REAL LspCap: the effect-language-service v0.87.2 CLI via spawnSync, severity=error filter, the honest absent fallback) + artifact-gate.ts 116L (EFFECT_ARTIFACT_GATE: write → diagnose → deny/allow + pta.intercept + journal) + the prepare patch (1-line additive, idempotent) + tests ×10 | The CLI probe ledger complete (binary/flags/output format); the clean/violating/absent trio green; the W3 scanner unchanged (the Tag identity reused); the tsconfig-plugin decision documented (kept OUT of the main tsconfig — 14 false-positive floatingEffects in tests would break tsc 0; per-test temp tsconfigs carry it) | CORRECT |

## 3. THE COVERAGE MAP (100%)
| S7P/S8L requirement | Deliverable | State |
|---|---|---|
| the TEST_EVASION pre-arm deny (the gate fixture) | the wraps suite's namesake test | PASS (in-container) |
| the Paragon tables with provenance | the provenance table (source paths + shas + §15 constants) | LANDED |
| the pre-arm chain order (L2 §4.12) | quoted + implemented in tool-engine | PASS |
| the REAL LspCap (criterion 14 substrate) | effect-lsp.ts v0.87.2 CLI wrap | LANDED |
| the EFFECT_ARTIFACT_GATE deny/allow | artifact-gate.ts + the tests | PASS |
| the prepare patch | package.json 1-line additive, idempotent | LANDED |
| the diagnostics token in the journal | the lsp-gate tests assert the journal row | PASS |
| zero regressions | the 211 baseline inside the 233 | PASS |

## 4. HONEST RESIDUALS
1. The language-service plugin is deliberately OUT of the main tsconfig (14 false-positive floatingEffects in legacy test callbacks); per-test temp tsconfigs carry it for gate diagnostics — the kernel-severity rollout on src/ lands with the W10 sweep.
2. The BehaviorEngine's decay uses seq-advance (the deterministic test clock), not wall-clock — the canon tables' production timing semantics; documented.
3. The bridge's ToolEngine consult is optional (backward compat) — the production wiring (always-bound) lands with W9's profiles.

## 5. THE THIRD [CT] — COMPLETED (shark-effect-kernels-w6, tarball dcabd97c)
T1 233/233 ✓ · T2 the battery 11/11 (pass 11 fail 0) ✓ · T3 mech-gate exit 0 ✓ · T4 wraps 12/12 ✓ · A1 corrupted→verified:false+exit1 ✓ · A2 usage exit 2 zero-[JESL ✓. Artifact: .trident/container-test-results.json (W6 section).
