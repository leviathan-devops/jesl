# RUNNING BUILD LOG — JESL

<!-- APPEND ONLY — NEVER EDIT OR DELETE PRIOR ENTRIES.

HOW TO USE THIS LOG DURING THE BUILD:

1. WHEN: Append an entry after EVERY major wave, feature completion, build
   event, or material learning — NOT just at milestones. This log updates at
   HIGHER FREQUENCY than every other canon doc.

2. WHY: This is the agent's "let me write this down while it's still fresh
   so I don't forget how/why I just did this" capture. The reasoning behind
   a build decision is perishable — record it in the moment, not at session
   end.

3. ENTRY FORMAT (append at the END of this file):
   ## [ISO-TIMESTAMP] — <wave/feature name>
   - WHAT: <what was built/done>
   - WHY: <the reasoning — the "how/why I did this" part>
   - HOW: <the approach, the key decisions, the dead ends>
   - EVIDENCE: <SHA, test result, passToken, artifact path>
   - NEXT: <what this unblocks or implies>

4. FREQUENCY RULE: every meaningful work unit gets an entry while it is
   still fresh. If you are about to start something new, the previous work
   unit should ALREADY have its entry.

5. NEVER: rewrite history, edit a prior entry, or skip the WHY. The WHY is
   the entire point of this log.
-->

## Entries
<!-- New entries append below this line. -->

## [2026-09-02T19:05+04] — W0 DOCS WAVE (CLOSED — gate 9/9 GREEN)
- WHAT: the Phase-2 additive docs surgery on the JESL canon. A1: bible v1.1→v1.2 (1,119L→1,595L, sha `68afa78e…`) — PART 2E EFFECT KERNEL (PHASE 2) inserted (2E.0–2E.12, 443L) between PART 2 and PART 3 + 6 in-place patches (§0.2 Effect grounding rows + pointers; §1F the Effect.forEach/Exit twin :100; §2.5 purity redefinition :251; 9G law table :1556; v1.2 row :1589). A2: DPL1 v1.0→v1.1-E (343L→477L, sha `35abb828…`) — §2.9 :172 (D9–D15, Effect contracts, RunContext caps:Context upgrade, 8-row TaggedError table, Driver Layers, Workflow.make idempotencyKey=docHash+seed) + §5.2 :357 (S1E–S8L + placement rule) + criteria 13–16 :392–395 + §8 Phase-2 chain :473 + v1.1-E marker :5.
- WHY: the authorized first wave (D25 BUILD GO) — the docs bind making W1's Effect-mandatory implementation dispatchable; the Phase-2 spec §2.1–2.2 IS the instruction; its §5 nine-questions gate is the merge authority.
- HOW: the prior session generated the 2 DPL1-grade prompts (.trident/waves/) and dispatched both trident_build agents, then the session crashed (provider cut) — A1 died at 43s pre-edit; A2 after 1 of 5 edits (safe partial: one additive line). RECOVERY: the crashed sessions were read from the opencode SQLite store (session-log-recovery); trident-wave-manager action=resume verified both persisted sessions and steered "continue" — both reloaded full context and completed. No prompt regenerated, no work redone.
- EVIDENCE: the orchestrator's independent battery: bible 1,595L · PART 2E :287 between :118/:731 · 2E.×44 · 9G :1556 · Effect.forEach ×13 · tokens 8/8 (5/4/7/8/14/1/1/1) · laws 1A–1I ×1; DPL1 477L · §2.9 :172 · §5.2 :357 · criteria 13–16 ×4 · v1.1-E ×2 · idempotencyKey · S1–S6 1/1/1. Manifest `396e4930`→`f77b448f`. Nine-questions 9/9. Audit: .trident/wave-audit/w0.md.
- NEXT: W1 unblocked — dispatched the same session (see the W1 entry).

## [2026-09-02T20:10+04] — W1 CORE ON EFFECT / S1E (CLOSED — S1E gate GREEN)
- WHAT: the greenfield jesl/ kernel tree — 5 parallel trident_build agents (wave-1788363307259-3d85d0), 36 .ts files, 3,810L, tree digest `12ffbb3d`. core/ 10 modules: schema 211L (WorkflowDoc codec, DI'd isKnownKind) · graph 222L (Kahn batches + cycle detection) · channels 158L (Ref+Deferred, wake) · bus 189L (glob EventBus, handler isolation, payload deepFreeze) · caps 133L (8 Tags matching DPL1 §2.9 driver strings + requireCaps → [JESL CAP-UNBOUND]) · errors 94L (the 8 frozen TaggedErrors + JESL_TOKENS) · executor 222L (runProgram, Exit-captured Effect.forEach concurrency 15, Clock timing, cap pre-flight NO invoke rows) · journal 277L (sha256 chain via HashCap injection, covers() replay, serialize/restore, FileSink interface) · registry 191L (append-only, 33 kinds) · evidence 160L (the 8-kind G1 port, RING_CAP 50, VERDICT_TTL_MS 5000). nodes/ 12 full + 21 stubs. tests/ 11 files, 104 it.effect units.
- WHY: S1E is the kernel substrate every later wave compiles against — D9–D15 landed as code, not prose.
- HOW: wave-manager generate→auto-dispatch; the frozen shared contracts (errors shapes, NodeResult/RunContext, constants) written into every prompt → the parallel merge compiled clean on first integration (tsc 0, zero import conflicts across 5 agents). Two documented cross-file surgical patches by agents to pass the gate (bus-caps: journal sigs; executor: bus casts + tsconfig types).
- EVIDENCE (the auditor's own runs on the merged tree): tsc 0 · vitest 104/104 · tokens byte-exact · manifest f77b448f (zero spec drift) · purity clean AFTER the auditor's 2 adversarial fixes: (1) Date.now in caps.ts:105 dummyClock → EffectClock.currentTimeMillis; (2) the invented 9th token [JESL GATE-FAIL] in gate.ts:49,62 → removed (the delta lives in the evidence anchor; the vocabulary is frozen at 8). THE DIAMOND: 3-node strict = 2 batches (graph.test) + 4-node = 2 batches with B/C overlap (executor.test). Audit: .trident/wave-audit/w1.md (VERDICT: CORRECT ×7, coverage 100%).
- LESSONS: (1) `bun test` DEAD for this tree — `npx vitest run` is the canon gate (bun:test ≠ @effect/vitest). (2) TestClock never auto-advances Effect.sleep — real timers in tests, Clock service in core. (3) effect 3.22.1 + platform-node 0.108.1 (peer warning benign). (4) Frozen-contracts-in-prompts = zero integration failures across 5 parallel agents.
- NEXT: W2 — CLI + EXECUTION (S2E): F12 the jesl bin (run/validate/replay; runPromise ONLY at the driver edge) + F13 the 4 execution kinds (shell-exec/python-exec/http-request/file-io — the first real cap bindings, CliLive). GATE: criteria 3,4,6,8 on the host.

## [2026-09-02T21:00+04] — W2 CLI + EXECUTION / S2E (CLOSED — S2E gate GREEN on the host, REAL path)
- WHAT: the jesl bin + the kernel's hands. cli/ (main.ts the single runPromise edge · args.ts · handlers.ts), the DPL1 §7.1 fixture set (8 files), drivers/cli-live.ts (REAL Shell via spawn+SIGKILL-timeout, Fs via fs/promises, Http via fetch, sha256 HashCap — node: confined to the driver), 4 real cap-bound execution nodes, the registry replaceStub seam (stub-only; append-only preserved), 25 new tests.
- WHY: S2E is the first runnable proof — criteria 3,4,6,8 are the host-level gates the whole build's credibility rests on.
- HOW: wave-manager dispatch (2 agents). THE AUDIT CAUGHT A FITTED-TO-GOLDEN MASK: the CLI's deterministic run path rode passHandle always-PASS stand-ins — behind it 3 REAL W1 defects: (1) schema NodeEnvelope stripped node config; (2) executor.ts:177 passed `inbound:{}` — nodes never saw channel data end-to-end; (3) the executor's fallback journal self was unverifiable by verifyChain. ALL THREE root-cause-fixed by the auditor (schema +config; executor full-envelope NodeInput + real channelData inbound + journal-algorithm self) + the mech-gate fixture redesigned for real semantics (one probe-error iteration: `$.triplet` object vs `$.triplet.state` string). Post-fix the PASS comes through the REAL kernel: 4 nodes, 8 chained rows, 4 batches.
- EVIDENCE (the auditor's own runs): tsc 0 · vitest 129/129 · mech-gate "verdict":"PASS" exit 0 (real path) · the 4 refusals exit 2 with exact tokens · needs-llm exit 2 [JESL CAP-UNBOUND] no artifact · replay verified:true exit 0 · runPromise ×1 · manifest f77b448f. Tree: 46 files, 5,028L, digest `e07eaf5c`. Audit: .trident/wave-audit/w2.md (w2-cli-bin recorded FLAWED→FIXED; the final tree CORRECT).
- LESSON (canon): a host-gate PASS is only evidence when the path through the REAL implementations is proven — an always-pass stand-in is the theatrical class the loud-fail law bans. W3+ audits attack the PASS path itself, every wave.
- NEXT: W3 — DRIVER + SCANNERS (S3E) [CT]: F14 the OpenCode driver Layer + the hook→bus bridge + jesl-run SessionLive · F15 the five scanners. GATE: criterion 11 + the ask-launcher roundtrip + THE FIRST CONTAINER CHECKPOINT (.trident/container-test-results.json rows).

## [2026-09-02T21:55+04] — W3 DRIVER + SCANNERS / S3E (code gates GREEN; the [CT] container leg BLOCKED with the mechanism documented)
- WHAT: the host seam + the police. drivers/opencode-live.ts (HostTransport{invokeTool,dispatchSubagent,callModel,ask} — the library's host-agnostic seam, ScriptedTransport for tests, no opencode SDK import), drivers/session-live.ts (Session Tag + Deferred/Queue ask — zero sleeps), drivers/hook-bridge.ts (onToolExecuteBefore → bus emit → the pta deny fold; per-bus WeakMap state; observer-law isolation), scanners/pba-banks.ts (the 5 family banks as PURE data copied from Paragon ms-ratio-classifier — the provenance table carries source shas + the survivor-table rows), scanners/pba.ts (6-pattern deliberation subscribe → detectFamilies → pba.family.hit), scanners/pta.ts (the pre-arm ring → deny-unless-armed per L2 §4.12 → pta.intercept), scanners/lsp.ts (LspCap Tag + InMemoryLsp + the honest available:false journal state), scanners/audit.ts (the evidence-triplet check → audit.violation), scanners/trace.ts (the per-run timeline → trace.timeline). 35 new tests.
- WHY: S3E is the host seam + the think/do police — the ask-launcher roundtrip (criterion 11) is the first interactive behavior; the scanners make Paragon's proven math enforceable on the kernel's bus without rewriting it (D12).
- HOW: wave-manager dispatch (3 agents, disjoint file sets; cross-agent coordination ONLY through the bus event contract: pba.family.hit / pta.intercept / lsp.diagnostics / audit.violation / trace.timeline). The Paragon banks were transcribed with provenance shas (the substrate is frozen/read-only — BUILD_STATE §6).
- EVIDENCE (the auditor's own runs): tsc 0 · vitest 164/164 · the roundtrip E2E (suspend → scripted answer → PASS + ask/answer rows) · unbound Session → [JESL CAP-UNBOUND] · the pre-arm chain deny-unless-armed · determinism ×100 · the 500-event burst stateless · the honest LSP unavailable · manifest f77b448f. THE SWEEP found + fixed: cli/main.ts usage errors wore [JESL UNKNOWN-NODE] (a token-semantics violation) → plain usage text. Tree digest `c154678b`. Audit: .trident/wave-audit/w3.md (3× CORRECT + the fix).
- THE [CT] BLOCK (the honest ledger): the container checkpoint is BLOCKED — opencode-test:1.14.34 absent + pull denied; the sandbox substitute boots without the tmux/opencode session (exec/deploy all tui_dead); raw docker firewalled; the setup sha check returns an empty container side. ALL 10 scenarios recorded BLOCKED in .trident/container-test-results.json with the mechanism + the unblock path (provision the rig image → setup → deploy → exec the scenarios from .trident/test-plan.md). The host evidence (my own runs) is marked SUPPORTING, never substituted.
- NEXT: W4 — GENERATION (S4E): F16 prompt dual-mode (call-model as Activity; unbracketed still refused) + F17 the jesl-run Workflow (Workflow.make, DurableDeferred ask-launcher, replay no-re-pay). GATE: S7 + no-re-pay on generation replay. The [CT] re-run rides the next checkpoint (W5) once the rig image is provisioned.

## [2026-09-02T23:00+04] — W4 GENERATION / S4E (CLOSED — gate GREEN)
- WHAT: the generation surface + the durability layer. nodes/prompt.ts 223L (the dual-mode Activity: llm via the Llm cap / template pure; the bracket repair≤2; confidence 0.55 → INCONCLUSIVE/UNCLEAR; journal pre+post rows; replaceStub at init) + workflow/jesl-run.ts 146L (Workflow.make JeslRun: payload docHash+seed, idempotencyKey `${docHash}:${hashSeed}`, RunReceipt; the restore-or-start handler: rows-present → verify loud-fail → covers-gated replay rebuilding the summary FROM rows → runProgram uncovered) + workflow/activities.ts 102L (Activity.make node:<id>; the DurableDeferred ask: the question row persists, the answer row resumes, counter 1 total) + 20 tests. Deps: @effect/workflow 0.19.1 + @effect/ai 0.37.0 installed (DD24 ledger: @effect/ai not imported — the Llm cap stands alone; the deltas vs the DPL1 sketch recorded).
- WHY: S4E is the kernel's only non-deterministic surface + its durability — the no-re-pay replay (criterion 15) is the generation law's proof.
- HOW: wave-manager dispatch (2 agents). The prompt agent patched the registry's ALL_KINDS gap ('prompt' missing — replaceStubSync would have failed) and reconciled cli/handlers.ts to the real node. The workflow agent's VERIFY-ON-INSTALL ledger maps every DPL1 sketch field to the installed surface (executionId = hash-of-key noted).
- EVIDENCE (the auditor's own runs): tsc 0 · vitest 184/184 · THE NO-RE-PAY: second run invoke counter 0 + covers true + verdict FROM rows · the ask suspend/resume counter 1 · the corrupt loud-fail · manifest f77b448f. THE SWEEP caught + fixed: [JESL JOURNAL-CORRUPT] ×5 (an invented 9th token, the W3 class recurring) → plain JOURNAL_CORRUPT strings. Tree: 64 files, digest `408f7500`. Audit: .trident/wave-audit/w4.md.
- NEXT: W5 — PACKAGER + BATTERY (S5E-S6E) [CT]: F20 the 3 emitters (plugin tool / tool-chain / skill directory) · F23-F25 the fixture battery + the artifact rows. GATE: 3 targets run · determinism · kill-resume + the SECOND [CT] container checkpoint.

## [2026-09-03T00:30+04] — W5 PACKAGER + BATTERY / S5E-S6E (CLOSED — gate GREEN + THE SECOND [CT] 11/11 IN-CONTAINER)
- WHAT: packager/{shared,tool,chain,skill}.ts (one doc → 3 targets: the plugin-tool manifest, the tool-chain descriptor, the skill rocket with the byte-preserved workflow.json; criterion 9 re-validation on every target; the injected SkillWriter — zero host imports in the emit cores) + tests/battery.ts (the executable 11-row battery runner writing the {rows,summary} artifact) + fixtures-kill-resume.json + 27 new tests (19 packager + 8 battery).
- WHY: S5E-S6E is the ship surface + the definition of done made executable — the rockets (D4) become drop-a-directory real.
- HOW: wave-manager dispatch (2 agents). THE AUDIT INTEGRATED THE TWO HALVES: the battery's S6 was written against an imagined artifact shape — the orchestrator rewired it to call the REAL emitSkill (parsed doc → emitSkill → run the emitted payload/workflow.json through handleRun → exit 0) — the two parallel agents' work now composes. THE BATTERY ALSO CAUGHT A REAL KERNEL DEFECT: canonicalSerialize included ts in the sha → determinism (criterion 5, Law 6) was broken — the surgical fix (journal.ts:44 excludes ts) landed and the determinism test proves chainsIdentical:true.
- EVIDENCE (the auditor's own runs): tsc 0 · vitest 211/211 (20 files) · THE BATTERY RUNNER: pass 11 fail 0 blocked 0 · THE SECOND [CT]: 211/211 + 11/11 rows in the clean container (shark-effect-kernels-w5, tarball aab37463) — S6 emits the rocket via the real packager and runs it exit 0 · manifest f77b448f · tokens clean (the 5 legit only). Audit: .trident/wave-audit/w5.md.
- NEXT: W6 — THE WRAPS (S7P+S8L) [CT]: the Paragon BehaviorEngine/ToolEngine Layers intercepting tool.execute.before (TEST_EVASION pre-arm) + the EffectLsp Activity + the PTA layer EFFECT_ARTIFACT_GATE + patch in prepare. GATE: S10 pre-arm · the diagnostics token + the THIRD [CT].

## [2026-09-03T01:00+04] — W6 THE WRAPS / S7P+S8L (CLOSED — gate GREEN + THE THIRD [CT] IN-CONTAINER)
- WHAT: the enforcement + artifact planes made real. wraps/behavior-engine.ts 194L (the arming state: the escalation windows 5/2/0 + refractory 25 + alpha 0.05 + fire 1.0 thr-v1 ported WITH Paragon provenance shas — ms-escalation-memory + sentinel/synapse), wraps/tool-engine.ts 72L (the intercept consult: armed → deny w/ family; unarmed bash → deny-unless-armed; clean → allow), wraps/effect-lsp.ts 126L (the REAL LspCap via the effect-language-service v0.87.2 CLI — spawnSync at the wrap boundary, severity=error filter, the honest absent fallback), wraps/artifact-gate.ts 116L (EFFECT_ARTIFACT_GATE: write → diagnose → deny/allow + the pta.intercept + journal rows), the hook-bridge surgical edit (the optional ToolEngine consult, backward compat preserved), the prepare patch (effect-language-service patch), 22 new tests.
- WHY: S7P/S8L make Paragon's proven enforcement + the artifact plane REAL — the pre-arm chain (L2 §4.12) executes in production form; criterion 14 (LSP diagnostics exit 0) becomes runnable.
- HOW: wave-manager dispatch (2 agents). THE PARAGON WRAP carries the full provenance table (source paths + shas per constant — D12 honored). The LSP agent probed the real CLI (v0.87.2, the JSON output format, the tsconfig-plugin requirement) and kept the plugin OUT of the main tsconfig (14 false-positive floatingEffects in tests would break tsc 0 — documented).
- EVIDENCE (the auditor's own runs): tsc 0 · vitest 233/233 (22 files) · tokens clean (zero [JESL inventions in wraps/) · manifest f77b448f. THE THIRD [CT] (shark-effect-kernels-w6, tarball dcabd97c): 233/233 · the battery 11/11 (pass 11 fail 0 blocked 0) · wraps 12/12 · mech-gate exit 0 · the corrupted journal → verified:false exit 1 · the usage errors token-clean. Audit: .trident/wave-audit/w6.md.
- NEXT: W7 — THE MPSE BRIDGE (P2): F18 parser/cards · oracle/calibrate · kernel/stub emit. GATE: the demo fixture compiles; D17 reproduced. W7 may run parallel-independent of W3-W6 (depends on W1 only — which is closed).

## [2026-09-03T01:50+04] — W9 PROFILES + BINDINGS / P4 (CLOSED — gate GREEN)
- WHAT: profiles/{shared,trident,trading,sales}.ts (the DomainModule DATA — zero branches, pure lookup tables) + bindings/{host-binding,opencode-binding}.ts (the ParagonHostBinding contract + the OpenCode binding Layer) + 16 tests.
- WHY: P4 is the domain composition + the host wiring — the profiles are the adoption presets, the bindings are the deployment surface.
- HOW: wave-manager dispatch (2 agents). The profiles agent self-caught the isRealTag function-vs-object bug. The bindings agent delegated to the proven makeOpenCodeLive.
- EVIDENCE (the auditor's runs): tsc 0 · vitest 330/330 (33 files) · zero branches (grep=0) · manifest f77b448f. Tree: 107 files, digest c96aeb38. Audit: .trident/wave-audit/w9.md.
- NEXT: W10 — the boilerplate extraction + the adoption dry-run (THE FINAL WAVE). Gate: 11/11 + the adoption dry-run + the FINAL [CT].

## [2026-09-03T01:55+04] — W10 SHIP — the extraction + the adoption dry-run (DISPATCHED — the FINAL wave)
- WHAT: the copy-and-customize proof — extractBoilerplate produces a new adoptable tree from the kernel + the profile injection.
- GATE: 11/11 battery + the adoption dry-run + the FINAL [CT] container checkpoint.

## [2026-09-03T02:20+04] — W10 SHIP (CLOSED — THE FINAL [CT] GATE IS GREEN — THE PRODUCT IS SHIPPABLE)
- WHAT: the boilerplate extraction (jesl/boilerplate/extraction.ts — extractBoilerplate copies the kernel tree + injects the profile + generates package.json/tsconfig) + the adoption dry-run (6 tests proving the extracted tree compiles, its core is byte-identical, its manifest validates, its battery would pass) + the W9 profiles/bindings integration.
- WHY: W10 is the DEFINITION OF DONE — the adoption dry-run proves the library is production-adoptable by a new project with zero source changes.
- HOW: wave-manager dispatch (1 agent). The auditor fixed the recurring 9th-token class ([JESL UNKNOWN-PROFILE] → plain UNKNOWN_PROFILE — the THIRD occurrence of this pattern across W3/W4/W10).
- EVIDENCE (the auditor's own runs + THE FINAL [CT]): tsc 0 · vitest 336/336 (34 files) · the battery 11/11 (pass:11 fail:0) · the boilerplate 6/6 · the corrupted journal → verified:false + exit 1 · the usage errors token-clean · manifest f77b448f · specs untouched through ALL 10 waves. Tree: 109 .ts files, digest `681bf869`, tarball `30fa9ec4` deployed to shark-effect-kernels-w10.
- THE SHIP GATE IS GREEN. THE PRODUCT IS SHIPPABLE.

## [2026-09-03T13:30+04] — THE DOC-COMPLETION PASS (the post-build documentation — CLOSED)
- WHAT: the 3 bible/manual deliverables filled + the D26 rename sweep. (1) JESL_KERNEL_ARCHITECTURE_BIBLE.md v1.1 — 1,786L, 28 PARTS: current state, one-breath, the macro diagram, the 10 core deep-dives, the node system, the drivers, the MPSE bridge, the 6 kernels, the packager+boilerplate, the scanners+wraps, profiles+bindings, the testing architecture (the per-file ledger), the evidence (the 12-finding table + the 28 container rows + the verification recipes), the normative contracts (DPL1 §2.9 verbatim + E1-E10 + 9G + P3-1..12), the slice/criteria tables, the 5K templates, the troubleshooting matrix, the threshold register, the data flows (the mech-gate journal walk), the per-kernel deep reference, the export surfaces, the IR table, the adoption guide, the debug ledger, the wave history, the test ledger, the config examples, the 2E map. (2) JESL_KERNEL_OPERATING_MANUAL.md v1.0 — 504L: all 5 SECTION skeletons filled (16 node-kind details with config/reads/writes/failures + anchors; the 3 drivers; the full RunContext/NodeHandle API; the 3 emitters + extraction; the lifecycle table). (3) the boilerplate ADOPTION.md (the 5-step walkthrough + the EDIT/FIXED surface + the invariants). (4) w9.md REBUILT (was truncated at 50L — §§1-5 reconstructed from RUNNING_BUILD_LOG/BUILD_REPORT/TASK_QUEUE canon; §§6-9 preserved verbatim; the truncation flagged in the file header).
- WHY: the handoff mandated the 3 deliverables; the operator ruled the wave-read pattern ("deploy an explore wave to absorb all this") — wave-1788426315484-ff7ff9 (6 trident_explore agents: kernels, tests, audits, specs, fixtures+boilerplate, doctrine) harvested COMPLETE; the primary synthesized the returns + its own 50-file source read into the docs.
- HOW: wave-manager generate (the template-kinship filter caught the kernel-dir-name verb collision — prose scrubbed, re-fired) → auto-dispatch 6/6 → all 6 returns complete and anchored → the docs synthesized. THE D26 SWEEP executed: grep found "JSON Event Scripting Library" in exactly 2 code-prose surfaces (jesl/cli/args.ts:52 + the boilerplate copy), ZERO test assertions (grep-verified pre-change) → both renamed via edit/sed → the 4 doc occurrences remaining are historical quotes (correct). CORRECTIONS the wave surfaced (folded into the docs): battery.ts is 461L not 448; @ts-nocheck is on 4 files (incl. battery.ts); the container artifact has 28 flat rows (the 33 was aggregation double-count); WAVE_PLAN.md is 57L.
- EVIDENCE: the bible 1,786L + the manual 504L + ADOPTION.md + w9.md 103L (wc -l runs in the final verification); tsc/vitest re-run post-rename (the final gate block); the spec manifest f77b448f recomputed UNTOUCHED.
- NEXT: the final verification battery; the canon is current (this entry + BUILD_STATE + CHANGELOG Session 5).
